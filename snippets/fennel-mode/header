# -*- mode: snippet -*-
# name: header
# key: header
# --
;;; `(buffer-name)` --- ${1:description}

;;; Commentary:
;; ${2:Comments}

;;; Code:

(var `(file-name-sans-extension (buffer-name))` {})

;;;
;; Functions

$0

`(file-name-sans-extension (buffer-name))`
;;; `(buffer-name)` ends here
